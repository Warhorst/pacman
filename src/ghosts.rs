use bevy::prelude::*;

pub struct Ghost;
pub struct Red;
pub struct Pink;
pub struct Cyan;
pub struct Orange;

pub struct GhostPlugin;

impl Plugin for GhostPlugin {
    fn build(&self, app: &mut AppBuilder) {

    }
}